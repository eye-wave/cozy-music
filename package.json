{
	"name": "cozy-music",
	"type": "module",
	"private": true,
	"workspaces": [
		"src-client"
	],
	"scripts": {
		"dev": "bunx tauri dev",
		"dev:client": "cd src-client && bun run dev",
		"build:client": "cd src-client && bun run build",
		"build": "bunx tauri build",
		"format": "cargo fmt -v && bunx -b biome format . --write",
		"lint": "cargo clippy && bunx -b biome lint ."
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.5",
		"@tauri-apps/cli": "^2.8.4",
		"@trivago/prettier-plugin-sort-imports": "^5.2.2",
		"@types/bun": "latest"
	},
	"module": "index.ts",
	"peerDependencies": {
		"typescript": "^5"
	},
	"trustedDependencies": [
		"@tailwindcss/oxide",
		"src-client"
	]
}

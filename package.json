{
	"name": "cozy-music",
	"type": "module",
	"private": true,
	"workspaces": [
		"src-client"
	],
	"scripts": {
		"dev": "bunx tauri dev",
		"dev:client": "cd src-client && bun run dev",
		"build:client": "cd src-client && bun run build",
		"build": "bunx tauri build",
		"format": "cargo fmt -v && biome format . --write",
		"lint": "cargo clippy && biome lint . --fix"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.3",
		"@tauri-apps/cli": "^2"
	}
}
